{
  "name": "petclaw",
  "version": "0.1.0",
  "description": "Browser pet that trains your OpenClaw config files",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\""
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
